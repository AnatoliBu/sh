# Sysadmin & Analytics Agent Knowledge Base

This directory is a temporary fallback because the currently exposed GitHub connector can write to `AnatoliBu/sh`, but does not expose a repository-creation endpoint.

The intended target is a separate private repository for authoritative source-of-truth references, agent skills, rules, and workflow templates for:

- system administration / SRE / DevOps / network operations;
- data analytics / product analytics / BI.

Once a private repository is created or exposed through the connector, this directory should be moved there.
