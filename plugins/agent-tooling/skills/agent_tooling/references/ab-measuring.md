<!-- generated by sysadmin-analytics-agent-kb/tools/build_plugin_from_domain.py from sysadmin-analytics-agent-kb/agent-tooling/patterns/ab-measuring.md — edit the source, not this copy -->

# Choosing tasks, judging results

## Blind tasks

Write them as the ticket a support engineer would forward — the subject must not learn which tool
is under test. Real examples from the the project set (`prompts/A..G.txt`):

- **A, identity:** «Клиент 9796910 жалуется, что сегодня в приложении всё ломалось… скажи, где
  именно сломалось — какой сервис, какая ошибка, во сколько.»
- **B, one anomalous line:** a pasted WARN with a `traceId` → «собери всё связанное из остальных
  сервисов, что было до и после».
- **D, evidence-first:** a client-history row with a 3.5 MB response truncated to a S3 link.
- **F, drill-to-source:** a clipped stacktrace → «покажи полный текст этого лога целиком».
- **G, inventory:** what indices/fields exist at all (the boring one that exposes tool-count bloat).

Coverage rules:

- one task per capability you claim (correlation · triage · fetch-full-doc · inventory);
- at least one that your tool should REFUSE or narrow (cost guards, ambiguity stops) — refusals are
  where weak agents get lost;
- at least one where the honest answer is "there is nothing here" — silent-empty is the most common
  agentic-tool lie;
- keep the tasks frozen across releases; a moved yardstick measures nothing.

## Ground truth

Precompute with direct queries into `ground_truth.json`, keyed per task, with the numbers a correct
answer must contain (doc counts, response-code histogram, the exact ids/traces that exist). Without
it "the agent answered well" is an opinion — and the strong model grading the run is biased toward
the tool it just built.

## Verdicts

Score each run on three axes, in this order:

1. **Correct?** — key facts from ground truth present, nothing invented. A fast wrong answer is the
   worst outcome and must be recorded as such.
2. **Reached via the tool?** — check the tool SET, not just the count. "Solved it, but with
   `SearchIndexTool` while your tool sat unused" means your tool did not earn its context budget.
3. **Cheap?** — calls, tokens, wall-clock. Only compare these between runs that are both correct.

## Variance is the default, not the exception

Two rounds is the minimum, and it only tells you whether a cell is stable. In the the project baseline C, D
and F flipped between attempts; after the fixes D produced the key trace in 1 run of 2 and F in 2 of
2. Report it as a fraction ("2/2", "1/2"), never as a single number. For a decision that matters,
n≥3–5 on that cell.

## What a result may and may not claim

| Observation | Claim you may make | Claim you may NOT make |
|---|---|---|
| duration 363s → 130s, hung call gone | this specific stall is fixed | "the tool is 3× faster" |
| trace appears in 1 of 2 runs, 0 of 2 before | the link is now reachable | "the tool now finds it" |
| task solved without your tool | the task is solvable without you | nothing about your tool |
| refusal followed by 9 manual calls | the refusal does not route the agent | that refusing was wrong |

Write the "did NOT prove" list into the report explicitly. In one re-measure that list held the
most valuable line: the new FetchDoc locator was never used once — an unverified feature.

## Report format

Into the task note: the before/after table (per task: calls · duration · what changed IN SUBSTANCE),
then two sections — **proved** and **did not prove** — then background contamination (a red verify
verdict, leftover plugins, a different stand) that a later reader must know about to compare
anything with these numbers.
