<!-- generated by sysadmin-analytics-agent-kb/tools/build_plugin_from_domain.py from sysadmin-analytics-agent-kb/agent-tooling/patterns/contract-capability-ir-guide.md — edit the source, not this copy -->

# Capability IR Guide

## Why an intermediate representation exists

OpenAPI, GraphQL, Protobuf, shell CLIs, and MCP expose different concepts. A common IR prevents generation logic from becoming tied to one transport and lets CLI, MCP, documentation, and tests share the same semantic model.

## Minimum fields

At minimum, every capability should define:

```text
identity    provider / domain / resource / action / version
schemas     input / output / errors
behavior    read-only / destructive / idempotent / retryable / long-running
transport   OpenAPI operation / GraphQL field / RPC method / shell command / MCP item
security    auth type / scopes / tenant or profile requirements
interfaces  CLI mapping / MCP exposure
semantics   intents / examples / preconditions / related capabilities
provenance  source / version / import timestamp / confidence
```

the machine-readable capability schema is the canonical machine-readable shape.

## Identity

Use a stable identifier independent of transport:

```text
provider.domain.resource.action
```

Changing an HTTP path or executable flag should not force a capability rename when the user-visible intent remains the same.

### Naming across targets

Use stable lowercase identifiers with one separator convention per target.

```text
Capability ID    provider.domain.resource.action
MCP tool name    provider_resource_action
CLI command      resource action
```

Names should describe user intent. Keep transport paths, HTTP verbs, RPC package names, and executable filenames in provenance rather than leaking them into the public interface unless they are already established user concepts.

## Taxonomy

- `provider`: owning product or system
- `domain`: optional business or technical grouping
- `resource`: primary object or context
- `action`: user-facing verb
- `intents`: natural-language requests that should retrieve this capability
- `aliases`: established alternate names

## Schemas

Use JSON Schema-compatible objects or references. The IR does not require every source to originate as JSON, only that generated interfaces can reason about bounded inputs and outputs.

Keep transport envelopes out of the normalized output when possible. For example, normalize HTTP headers and pagination metadata separately rather than forcing every CLI user to consume the raw response envelope.

## Behavior

Behavioral fields are claims requiring evidence:

- `readOnly`: execution does not mutate relevant state
- `destructive`: execution can delete, overwrite, revoke, or irreversibly alter state
- `idempotent`: repeating the same invocation has the same intended state effect
- `retryable`: automatic retry is safe under declared conditions
- `longRunning`: execution may return a job, operation, or delayed result
- `streaming`: input or output is streamed
- `openWorld`: execution affects systems or parties outside the local controlled context
- `confirmation`: interaction policy before execution

Do not derive these mechanically from protocol verbs alone.

### Risk and confirmation

Classify every executable capability:

- read-only
- reversible mutation
- destructive mutation
- external communication
- financial or billing effect
- permission or identity change
- long-running or expensive operation

Assign one confirmation policy:

- none
- conditional
- always
- prohibited in unattended mode

Never infer read-only status from `GET` alone or idempotency from `PUT` alone. Verify documented and observed behavior.

## Transport

The transport block preserves how the operation is actually called. It may contain transport-specific extension fields. Generators should read only the fields needed for their adapter.

## Security

Represent required auth mechanisms and scopes without embedding secrets. Keep environment resolution in runtime configuration.

## CLI

The CLI block is an interface projection, not the implementation. It declares command path, positional inputs, flags, stdin support, output formats, and dry-run capability.

## MCP

The MCP block defines whether the capability is exposed and whether it becomes a Tool, Resource, or Prompt. `retrievalOnly` means the capability remains in the registry but is not advertised in the default static tool set.

## Provenance and confidence

Record whether important semantics were declared by the source or inferred. Suggested confidence values:

- `declared`
- `inferred-high`
- `inferred-medium`
- `inferred-low`
- `unknown`

Evidence should point to source operations, help sections, documentation headings, tests, or observed behavior.
