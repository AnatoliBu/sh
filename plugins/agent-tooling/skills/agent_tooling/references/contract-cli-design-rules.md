<!-- generated by sysadmin-analytics-agent-kb/tools/build_plugin_from_domain.py from sysadmin-analytics-agent-kb/agent-tooling/patterns/contract-cli-design-rules.md — edit the source, not this copy -->

# CLI Design Rules

## Command hierarchy

Prefer resource-oriented commands:

```text
tool issue create
tool issue list
tool issue close
```

Avoid transport-derived commands:

```text
tool post-repos-owner-repo-issues
```

## Inputs

- Use positional arguments for one or two obvious identifiers.
- Use flags for optional, repeated, ambiguous, or sensitive values.
- Support stdin or file input for large text and structured payloads.
- Never require a TTY for automation unless the operation is explicitly interactive.
- Provide an explicit non-interactive confirmation mechanism for unattended use.

## Outputs

- Human-readable text is the interactive default.
- JSON is mandatory for automation-oriented commands.
- Use JSON Lines for streaming collections when appropriate.
- Send result data to stdout.
- Send warnings, progress, and diagnostics to stderr.
- Keep stable field names in structured output.

## Exit behavior

Define stable categories such as:

```text
0 success
2 usage or validation error
3 authentication or authorization error
4 not found
5 conflict or precondition failure
6 transient remote failure
7 permanent remote failure
8 partial success
```

Exact numbers may differ by project, but the mapping must be documented and consistent.

## Safety

- Risky commands should support `--dry-run` when technically meaningful.
- Destructive commands should require explicit confirmation or a force flag.
- Do not accept a broad force flag that silently confirms unrelated risks.
- Print the exact target before destructive execution.

## Pagination and long-running work

- Provide automatic pagination plus explicit page controls.
- Provide bounded result limits.
- For long-running operations, expose operation ID, status, wait, and cancel semantics when available.

## Configuration

Use a consistent precedence order, for example:

```text
flags > environment > profile/config file > defaults
```

Do not expose secrets in process listings when stdin, environment, keychain, or credential helpers are available.
